import { UpcomingDaysForecast } from './upcoming-days-forecast';

describe('UpcomingDaysForecast', () => {
  it('should create an instance', () => {
    expect(new UpcomingDaysForecast()).toBeTruthy();
  });
});
