import { WeatherTemperature } from './weather-temperature';

describe('WeatherTemperature', () => {
  it('should create an instance', () => {
    expect(new WeatherTemperature()).toBeTruthy();
  });
});
