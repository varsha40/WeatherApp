import { AmPmFormatPipe } from './am-pm-format.pipe';

describe('AmPmFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AmPmFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
