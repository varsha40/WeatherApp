import { ExtracttimePipe } from './extracttime.pipe';

describe('ExtracttimePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtracttimePipe();
    expect(pipe).toBeTruthy();
  });
});
