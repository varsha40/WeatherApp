import { LocaltimetodayPipe } from './localtimetoday.pipe';

describe('LocaltimetodayPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaltimetodayPipe();
    expect(pipe).toBeTruthy();
  });
});
